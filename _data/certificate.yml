title: Certificate authority

steps:
  - title: Install certbot-auto
    class: is-ubuntu-14
    code: |
      ```sh
      wget https://dl.eff.org/certbot-auto
      chmod a+x certbot-auto
      mv certbot-auto /usr/local/bin/
      ```

  - title: Install letsencrypt
    class: is-ubuntu-16
    code: |
      ```sql
      apt-get install letsencrypt
      ```

  - title: Create the certificate
    class: is-ubuntu-14 is-apache
    code: |
      ```sh
      service apache2 stop
      certbot-auto certonly --standalone --agree-tos --renew-by-default -d example.com
      ```

  - title: Create the certificate
    class: is-ubuntu-16 is-apache
    code: |
      ```sql
      service apache2 stop
      letsencrypt certonly --standalone --agree-tos --renew-by-default -d example.com
      ```
